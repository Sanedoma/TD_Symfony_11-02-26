{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Nouvelle catégorie{% endblock %}

{% block body %}
<div class="example-wrapper" style="margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif;">
    <h1>Créer une nouvelle Catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_category') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
